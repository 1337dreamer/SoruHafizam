<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight">

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--<style name="EditTextTheme" parent="Theme.AppCompat.DayNight">
        <item name="colorControlNormal">@color/backgroundTint</item>
        <item name="colorControlActivated">@color/red</item>
        <item name="colorControlHighlight">@color/kara</item>
    </style>-->
    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:textColor">@color/whitee</item>
        <item name="android:editTextColor">@color/kara</item>
        <item name="android:textColorHint">@color/searchHint</item>
    </style>

</resources>